namespace ExtensionMethods
{
    // Instance Methods <--
    // static Methdos <--
    // Extenstion Methods <--
    // Partial Methods
    // Abstract Methods
    // Virtual Methods
    internal class Program
    {
        static void Main(string[] args)
        {
            string name = "usman";
            Console.WriteLine(name.SayHello());
        }
    }

    /* learnings
     * 
     * - Extension methods is static lives inside a static class
     * - When we can't or don't want to extend a class we make extension methods
     * */
    
}